import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
  importJobs: {
    width: "100%",
    paddingBottom: "2rem",
  },
  filter: {
    // marginTop: "2rem",
  },
  bottomAction: {
    padding: "1rem",
    display: "flex",
    justifyContent: "flex-end",
    alignItems: "center",
  },
}));
